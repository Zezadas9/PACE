node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.vite/

# Native projects, once `npx cap add` has been run.
/ios/
/android/

# The pre-React version, kept locally for reference only.
/legacy/

# Editor and tooling scratch.
.claude/

# Segredos locais do Worker e estado do Wrangler.
worker/.dev.vars
worker/node_modules/
worker/.wrangler/
.wrangler/

# Variáveis do frontend (o .env.example é que fica versionado).
.env
.env.local
.env.*.local
